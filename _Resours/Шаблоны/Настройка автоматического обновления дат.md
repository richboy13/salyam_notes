---
title: Настройка автоматического обновления дат в Obsidian
created: 2025-08-23
modified: 2025-09-13
aliases:
  - автоматическое обновление дат
  - плагины obsidian
type: guide
---

# ⚙️ Настройка автоматического обновления дат в Obsidian

## 📋 Описание
Инструкция по настройке автоматического обновления дат в YAML свойствах при редактировании файлов.

## 🔧 Плагин Templater

### 📥 Установка
1. Откройте **Настройки** → **Сообщество**
2. Отключите **Безопасный режим**
3. Найдите плагин **Templater**
4. Установите и включите

### ⚙️ Настройка
1. **Настройки** → **Templater**
2. **Template folder location** → выберите папку с шаблонами
3. **Trigger Templater on new file creation** → включите
4. **Enable Folder Templates** → включите

## 📝 Шаблон с автоматическими датами

### 🔄 Создайте файл `_Resours/Шаблоны/Автоматический шаблон.md`:

```yaml
---
title: "{{title}}"
date: <% tp.date.now() %>
created: <% tp.file.creation_date() %>
modified: <% tp.file.last_modified_date() %>
tags: []
aliases: []
status: draft
type: note
---
```

### ⚡ Функции Templater:
- `<% tp.date.now() %>` - текущая дата
- `<% tp.file.creation_date() %>` - дата создания файла
- `<% tp.file.last_modified_date() %>` - дата последнего изменения

## 🔄 Автоматическое обновление modified

### 📝 Создайте скрипт `update_modified.js`:

```javascript
// Скрипт для автоматического обновления modified даты
module.exports = async function(tp) {
    const file = tp.file.find_tfile(tp.file.title(true));
    const frontmatter = app.metadataCache.getFileCache(file).frontmatter;
    
    if (frontmatter) {
        frontmatter.modified = tp.date.now();
        await app.fileManager.processFrontMatter(file, (fm) => {
            fm.modified = tp.date.now();
        });
    }
    
    return tp.date.now();
}
```

### 🔗 Использование в шаблоне:
```yaml
---
title: "{{title}}"
date: <% tp.date.now() %>
created: <% tp.file.creation_date() %>
modified: <% tp.file.last_modified_date() %>
last_updated: <% tp.file.last_modified_date() %>
---
```

## 🎯 Альтернативные плагины

### 📅 **Dataview**
- Автоматическое отслеживание дат
- Создание динамических списков по датам

### 🔄 **Periodic Notes**
- Автоматическое создание заметок по расписанию
- Встроенное отслеживание дат

### ⚡ **QuickAdd**
- Быстрое создание заметок с автоматическими датами
- Интеграция с Templater

## 💡 Рекомендуемая настройка

### 1️⃣ **Для новых файлов:**
```yaml
---
title: "{{title}}"
date: <% tp.date.now() %>
created: <% tp.file.creation_date() %>
modified: <% tp.file.last_modified_date() %>
tags: []
aliases: []
status: draft
type: note
---
```

### 2️⃣ **Для существующих файлов:**
- Используйте реальные даты (как мы сделали)
- Templater будет автоматически обновлять `modified`

### 3️⃣ **Автоматическое обновление:**
- Templater обновляет даты при каждом сохранении
- Можно настроить горячие клавиши для быстрого обновления

## 🔗 Связи
- [[Templater]]
- [[Плагины Obsidian]]
- [[Автоматизация]]

## 🏷️ Теги
#настройка #obsidian #плагины #автоматизация #даты

---