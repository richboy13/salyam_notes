---
title: "Трекер тренировок"
created: 2024-01-15
modified: 2024-01-15
---

# 📅 Трекер тренировок

## 📋 Описание
Основной трекер для отслеживания тренировок, весов и прогресса. Использует DataviewJS для автоматического анализа данных.

## 🎯 Цели на 6 месяцев

### ⚖️ Вес и измерения
- **Вес:** 57-58 кг (чистая масса)
- **Бицепс:** +2-3 см
- **Грудь:** +3-4 см
- **Талия:** поддерживать текущую

### 🏋️‍♂️ Силовые показатели
- **Жим лёжа:** 60 кг × 8
- **Тяга штанги:** 60 кг × 10
- **Жим ногами:** 130 кг × 10
- **Ягодичный мост:** 85 кг × 10
- **Жим стоя:** 40 кг × 8
- **Планка:** 3 минуты

## 📊 Текущий прогресс

### 🏋️‍♂️ Силовые показатели
```dataviewjs
// Простой тест - показываем только количество файлов
const pages = dv.pages('"Dairy/Дневник/Тренировки"')
  .where(p => p.жим_лежа || p.тяга_штанги || p.жим_ногами || p.ягодичный_мост || p.жим_стоя);

dv.paragraph(`Найдено файлов с данными: ${pages.length}`);

if (pages.length > 0) {
  dv.paragraph("Последние записи:");
  for (let p of pages) {
    dv.paragraph(`- ${p.file.name}: Жим лёжа ${p.жим_лежа || 0} кг`);
  }
}
```

### ⚖️ Измерения тела
```dataviewjs
// Простой тест - показываем только количество файлов
const measurements = dv.pages('"Dairy/Дневник/Тренировки"')
  .where(p => p.вес || p.бицепс_левый || p.бицепс_правый || p.грудь || p.талия);

dv.paragraph(`Найдено файлов с измерениями: ${measurements.length}`);

if (measurements.length > 0) {
  dv.paragraph("Последние измерения:");
  for (let p of measurements) {
    dv.paragraph(`- ${p.file.name}: Вес ${p.вес || 0} кг`);
  }
}
```

## 📈 Статистика тренировок

### 🗓️ Частота тренировок
```dataviewjs
// Подсчитываем количество тренировок
const recentWorkouts = dv.pages('"Dairy/Дневник/Тренировки"')
  .where(p => p.тренировка);

const workoutCount = recentWorkouts.length;
const daysInMonth = 30;
const frequency = ((workoutCount / daysInMonth) * 100).toFixed(1);

dv.header(3, "📊 Статистика тренировок");
dv.paragraph(`**Всего тренировок:** ${workoutCount}`);
dv.paragraph(`**Частота:** ${frequency}% (${workoutCount}/${daysInMonth} дней)`);
```

### 🎯 Прогресс к целям
```dataviewjs
// Получаем последние показатели
const latest = dv.pages('"Dairy/Дневник/Тренировки"')
  .where(p => p.жим_лежа || p.тяга_штанги || p.жим_ногами || p.ягодичный_мост || p.жим_стоя || p.вес)
  .sort(p => p.file.name, 'desc')
  .first();

if (latest) {
  dv.header(3, "🎯 Прогресс к целям");
  
  const goals = [
    { name: "Жим лёжа", current: latest.жим_лежа || 0, target: 60 },
    { name: "Тяга штанги", current: latest.тяга_штанги || 0, target: 60 },
    { name: "Жим ногами", current: latest.жим_ногами || 0, target: 130 },
    { name: "Ягодичный мост", current: latest.ягодичный_мост || 0, target: 85 },
    { name: "Жим стоя", current: latest.жим_стоя || 0, target: 40 },
    { name: "Вес", current: latest.вес || 0, target: 57.5 }
  ];
  
  for (let goal of goals) {
    if (goal.current > 0) {
      const progress = ((goal.current / goal.target) * 100).toFixed(1);
      const bar = "█".repeat(Math.floor(progress / 10)) + "░".repeat(10 - Math.floor(progress / 10));
      dv.paragraph(`**${goal.name}:** ${goal.current}/${goal.target} (${progress}%) ${bar}`);
    }
  }
}
```

## 📝 Шаблон для новой тренировки

### 📅 Дата тренировки
```yaml
---
title: "Тренировка YYYY-MM-DD"
created: 2025-08-25
modified: 2025-08-29
тренировка: true
силовая_тренировка: true
единоборства: false
плавание: false
растяжка: true
жим_лежа: 40
тяга_штанги: 35
жим_ногами: 55
ягодичный_мост: 50
жим_стоя: 25
вес: 55
бицепс_левый: 30
бицепс_правый: 30
грудь: 95
талия: 75
завтрак: true
обед: true
ужин: true
перекус: true
белки: 3
углеводы: 3
жиры: 1
овощи: 3
протеин: true
креатин: true
bcaa: false
---
```

## 🔗 Связи
- [[📊 Аналитика прогресса]]
- [[🥗 Трекер питания]]
- [[📈 Трекер весов]]

## 📌 Заметки
<!-- Дополнительные заметки -->

## 🏷️ Теги
#тренировки #трекер #прогресс #dataview

---
