---
title: "Трекер питания"
created: 2024-01-15
modified: 2024-01-15
---

# 🥗 Трекер питания

## 📋 Описание
Трекер питания для отслеживания приемов пищи, макронутриентов и спортивных добавок. Без подсчета калорий, фокус на качестве питания.

## 🎯 Принципы питания

### ✅ Основа питания
- **Белки (2-3 раза в день):** курица, индейка, говядина, рыба, яйца, творог
- **Углеводы (3 раза в день):** рис, гречка, макароны, хлеб из цельного зерна, картошка
- **Жиры (ежедневно):** орехи, семена, авокадо, оливковое масло
- **Овощи и зелень** — к каждому приёму пищи

### ⚡ Спортивные добавки
- **Протеин:** шейк сразу после зала
- **Креатин:** ежедневно 5 г
- **BCAA:** можно пить во время или после зала (не обязательно)
- **Батончик/гейнер:** если нет еды

### 👉 Простой принцип
Ешь белок в каждом приёме пищи + углеводы до и после тренировок.

## 📊 Аналитика питания

### 🍽️ Приемы пищи за последние 7 дней
```dataviewjs
// Получаем записи за последние 7 дней
const recentMeals = dv.pages('"Dairy/Дневник/Тренировки"')
  .where(p => (p.завтрак || p.обед || p.ужин || p.перекус))
  .sort(p => p.file.name, 'desc')
  .limit(7);

dv.paragraph(`Найдено записей питания: ${recentMeals.length}`);

if (recentMeals.length > 0) {
  dv.paragraph("Последние приемы пищи:");
  for (let p of recentMeals) {
    const meals = [];
    if (p.завтрак) meals.push("Завтрак");
    if (p.обед) meals.push("Обед");
    if (p.ужин) meals.push("Ужин");
    if (p.перекус) meals.push("Перекус");
    dv.paragraph(`- ${p.file.name}: ${meals.join(", ") || "Нет данных"}`);
  }
}
```

### 🥩 Макронутриенты за последние 7 дней
```dataviewjs
// Показываем макронутриенты
dv.paragraph("Макронутриенты:");
for (let p of recentMeals) {
  const macros = [];
  if (p.белки) macros.push(`Белки: ${p.белки} приема`);
  if (p.углеводы) macros.push(`Углеводы: ${p.углеводы} приема`);
  if (p.жиры) macros.push(`Жиры: ${p.жиры} приема`);
  if (p.овощи) macros.push(`Овощи: ${p.овощи} приема`);
  dv.paragraph(`- ${p.file.name}: ${macros.join(", ") || "Нет данных"}`);
}
```

### ⚡ Добавки за последние 7 дней
```dataviewjs
// Показываем добавки
dv.paragraph("Спортивные добавки:");
for (let p of recentMeals) {
  const supplements = [];
  if (p.протеин) supplements.push("Протеин");
  if (p.креатин) supplements.push("Креатин");
  if (p.bcaa) supplements.push("BCAA");
  dv.paragraph(`- ${p.file.name}: ${supplements.join(", ") || "Нет добавок"}`);
}
```

## 📈 Статистика питания

### 🗓️ Регулярность питания
```dataviewjs
// Подсчитываем статистику за последние 30 дней
const recentNutrition = dv.pages('"Dairy/Дневник/Тренировки"')
  .where(p => (p.завтрак || p.обед || p.ужин || p.перекус));

const totalDays = recentNutrition.length;
let breakfastCount = 0;
let lunchCount = 0;
let dinnerCount = 0;
let snackCount = 0;
let proteinCount = 0;
let carbCount = 0;
let fatCount = 0;
let veggieCount = 0;

for (let p of recentNutrition) {
  if (p.завтрак) breakfastCount++;
  if (p.обед) lunchCount++;
  if (p.ужин) dinnerCount++;
  if (p.перекус) snackCount++;
  if (p.белки && p.белки >= 2) proteinCount++;
  if (p.углеводы && p.углеводы >= 3) carbCount++;
  if (p.жиры && p.жиры >= 1) fatCount++;
  if (p.овощи && p.овощи >= 3) veggieCount++;
}

dv.header(3, "📊 Статистика питания за последние 30 дней");
dv.paragraph(`**Дней с записями:** ${totalDays}/30`);
dv.paragraph(`**Завтраки:** ${breakfastCount}/${totalDays} (${totalDays > 0 ? ((breakfastCount/totalDays)*100).toFixed(1) : 0}%)`);
dv.paragraph(`**Обеды:** ${lunchCount}/${totalDays} (${totalDays > 0 ? ((lunchCount/totalDays)*100).toFixed(1) : 0}%)`);
dv.paragraph(`**Ужины:** ${dinnerCount}/${totalDays} (${totalDays > 0 ? ((dinnerCount/totalDays)*100).toFixed(1) : 0}%)`);
dv.paragraph(`**Перекусы:** ${snackCount}/${totalDays} (${totalDays > 0 ? ((snackCount/totalDays)*100).toFixed(1) : 0}%)`);

dv.header(4, "🥩 Макронутриенты");
dv.paragraph(`**Белки (2+ приема):** ${proteinCount}/${totalDays} (${totalDays > 0 ? ((proteinCount/totalDays)*100).toFixed(1) : 0}%)`);
dv.paragraph(`**Углеводы (3+ приема):** ${carbCount}/${totalDays} (${totalDays > 0 ? ((carbCount/totalDays)*100).toFixed(1) : 0}%)`);
dv.paragraph(`**Жиры (1+ прием):** ${fatCount}/${totalDays} (${totalDays > 0 ? ((fatCount/totalDays)*100).toFixed(1) : 0}%)`);
dv.paragraph(`**Овощи (3+ приема):** ${veggieCount}/${totalDays} (${totalDays > 0 ? ((veggieCount/totalDays)*100).toFixed(1) : 0}%)`);
```

### ⚡ Статистика добавок
```dataviewjs
let proteinCount = 0;
let creatineCount = 0;
let bcaaCount = 0;

for (let p of recentNutrition) {
  if (p.протеин) proteinCount++;
  if (p.креатин) creatineCount++;
  if (p.bcaa) bcaaCount++;
}

dv.header(4, "⚡ Добавки");
dv.paragraph(`**Протеин:** ${proteinCount}/${totalDays} (${totalDays > 0 ? ((proteinCount/totalDays)*100).toFixed(1) : 0}%)`);
dv.paragraph(`**Креатин:** ${creatineCount}/${totalDays} (${totalDays > 0 ? ((creatineCount/totalDays)*100).toFixed(1) : 0}%)`);
dv.paragraph(`**BCAA:** ${bcaaCount}/${totalDays} (${totalDays > 0 ? ((bcaaCount/totalDays)*100).toFixed(1) : 0}%)`);
```

## 📝 Шаблон для записи питания

### 🍽️ Запись питания
```yaml
---
title: "Питание YYYY-MM-DD"
created: 2025-08-25
modified: 2025-08-29
завтрак: true
обед: true
ужин: true
перекус: true
белки: 3
углеводы: 3
жиры: 1
овощи: 3
протеин: true
креатин: true
bcaa: false
---
```

## 🥗 Примеры приемов пищи

### 🍳 Завтрак
- **Белки:** яйца, творог
- **Углеводы:** овсянка, хлеб
- **Жиры:** орехи, масло
- **Овощи:** помидоры, огурцы

### 🍽️ Обед
- **Белки:** курица, рыба
- **Углеводы:** рис, гречка
- **Жиры:** авокадо, оливковое масло
- **Овощи:** салат, овощи

### 🍲 Ужин
- **Белки:** говядина, индейка
- **Углеводы:** макароны, картошка
- **Жиры:** семена, орехи
- **Овощи:** зелень, овощи

### 🥜 Перекус
- **Белки:** протеин, творог
- **Углеводы:** фрукты, хлеб
- **Жиры:** орехи, семена
- **Овощи:** морковь, сельдерей

## 🔗 Связи
- [[📅 Трекер тренировок]]
- [[📊 Аналитика прогресса]]

## 📌 Заметки
<!-- Дополнительные заметки -->

## 🏷️ Теги
#питание #трекер #макронутриенты #добавки

---
