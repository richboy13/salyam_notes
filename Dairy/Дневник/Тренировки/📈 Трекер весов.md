---
title: "Трекер весов"
created: 2024-01-15
modified: 2024-01-15
---

# 📈 Трекер весов

## 📋 Описание
Детальный трекер весов для основных упражнений с целями и прогрессией. Фокус на функциональной массе и силе.

## 🎯 Цели на 6 месяцев

### 🏋️‍♂️ Силовые показатели
| Упражнение         | Текущий    | Цель        | Прогрессия                |
| ------------------ | ---------- | ----------- | ------------------------- |
| **Жим лёжа**       | 40 кг × 10 | 60 кг × 8   | +2.5 кг каждые 1-2 недели |
| **Тяга штанги**    | 35 кг × 10 | 60 кг × 10  | +2.5 кг каждые 1-2 недели |
| **Жим ногами**     | 55 кг × 10 | 130 кг × 10 | +5 кг каждые 1-2 недели   |
| **Ягодичный мост** | 50 кг × 10 | 85 кг × 10  | +5 кг каждые 1-2 недели   |
| **Жим стоя**       | 25 кг × 10 | 40 кг × 8   | +2.5 кг каждые 1-2 недели |

### ⚖️ Измерения тела
| Показатель        | Текущий | Цель                 |
| ----------------- | ------- | -------------------- |
| **Вес**           | 55 кг   | 57-58 кг             |
| **Бицепс левый**  | 30 см   | 32-33 см             |
| **Бицепс правый** | 30 см   | 32-33 см             |
| **Грудь**         | 95 см   | 98-99 см             |
| **Талия**         | 75 см   | 75 см (поддерживать) |

## 📊 Прогресс по упражнениям

### 🏋️‍♂️ Жим лёжа (грудь, трицепс)
```dataviewjs
// Получаем данные по жиму лёжа
const benchPress = dv.pages('"Dairy/Дневник/Тренировки"')
  .where(p => p.жим_лежа)
  .sort(p => p.file.name, 'desc')
  .limit(10);

if (benchPress.length > 0) {
  dv.header(3, "🏋️‍♂️ Жим лёжа - Прогресс");
  for (let p of benchPress) {
    const progress = p.жим_лежа >= 60 ? "🎯 Достигнуто!" : `${((p.жим_лежа/60)*100).toFixed(1)}%`;
    dv.paragraph(`- ${p.file.name}: ${p.жим_лежа} кг (${progress})`);
  }
}
```

### 🏋️‍♂️ Тяга штанги в наклоне (спина)
```dataviewjs
// Получаем данные по тяге штанги
const deadlift = dv.pages('"Dairy/Дневник/Тренировки"')
  .where(p => p.тяга_штанги)
  .sort(p => p.file.name, 'desc')
  .limit(10);

if (deadlift.length > 0) {
  dv.header(3, "🏋️‍♂️ Тяга штанги - Прогресс");
  for (let p of deadlift) {
    const progress = p.тяга_штанги >= 60 ? "🎯 Достигнуто!" : `${((p.тяга_штанги/60)*100).toFixed(1)}%`;
    dv.paragraph(`- ${p.file.name}: ${p.тяга_штанги} кг (${progress})`);
  }
}
```

### 🏋️‍♂️ Жим ногами (ноги)
```dataviewjs
// Получаем данные по жиму ногами
const legPress = dv.pages('"Dairy/Дневник/Тренировки"')
  .where(p => p.жим_ногами)
  .sort(p => p.file.name, 'desc')
  .limit(10);

if (legPress.length > 0) {
  dv.header(3, "🏋️‍♂️ Жим ногами - Прогресс");
  for (let p of legPress) {
    const progress = p.жим_ногами >= 130 ? "🎯 Достигнуто!" : `${((p.жим_ногами/130)*100).toFixed(1)}%`;
    dv.paragraph(`- ${p.file.name}: ${p.жим_ногами} кг (${progress})`);
  }
}
```

### 🏋️‍♂️ Ягодичный мост (ягодицы)
```dataviewjs
// Получаем данные по ягодичному мосту
const hipThrust = dv.pages('"Dairy/Дневник/Тренировки"')
  .where(p => p.ягодичный_мост)
  .sort(p => p.file.name, 'desc')
  .limit(10);

if (hipThrust.length > 0) {
  dv.header(3, "🏋️‍♂️ Ягодичный мост - Прогресс");
  for (let p of hipThrust) {
    const progress = p.ягодичный_мост >= 85 ? "🎯 Достигнуто!" : `${((p.ягодичный_мост/85)*100).toFixed(1)}%`;
    dv.paragraph(`- ${p.file.name}: ${p.ягодичный_мост} кг (${progress})`);
  }
}
```

### 🏋️‍♂️ Жим стоя (плечи)
```dataviewjs
// Получаем данные по жиму стоя
const overheadPress = dv.pages('"Dairy/Дневник/Тренировки"')
  .where(p => p.жим_стоя)
  .sort(p => p.file.name, 'desc')
  .limit(10);

if (overheadPress.length > 0) {
  dv.header(3, "🏋️‍♂️ Жим стоя - Прогресс");
  for (let p of overheadPress) {
    const progress = p.жим_стоя >= 40 ? "🎯 Достигнуто!" : `${((p.жим_стоя/40)*100).toFixed(1)}%`;
    dv.paragraph(`- ${p.file.name}: ${p.жим_стоя} кг (${progress})`);
  }
}
```

## 📊 Измерения тела

### ⚖️ Динамика веса и измерений
```dataviewjs
// Получаем последние измерения
const measurements = dv.pages('"Dairy/Дневник/Тренировки"')
  .where(p => p.вес || p.бицепс_левый || p.бицепс_правый || p.грудь || p.талия)
  .sort(p => p.file.name, 'desc')
  .limit(10);

if (measurements.length > 0) {
  dv.header(3, "⚖️ Измерения тела");
  for (let p of measurements) {
    const measures = [];
    if (p.вес) measures.push(`Вес: ${p.вес} кг`);
    if (p.бицепс_левый) measures.push(`Бицепс Л: ${p.бицепс_левый} см`);
    if (p.бицепс_правый) measures.push(`Бицепс П: ${p.бицепс_правый} см`);
    if (p.грудь) measures.push(`Грудь: ${p.грудь} см`);
    if (p.талия) measures.push(`Талия: ${p.талия} см`);
    dv.paragraph(`- ${p.file.name}: ${measures.join(", ") || "Нет данных"}`);
  }
}
```

## 🎯 Анализ прогресса

### 📈 Общий прогресс к целям
```dataviewjs
// Получаем последние показатели
const latest = dv.pages('"Dairy/Дневник/Тренировки"')
  .where(p => p.жим_лежа || p.тяга_штанги || p.жим_ногами || p.ягодичный_мост || p.жим_стоя || p.вес)
  .sort(p => p.file.name, 'desc')
  .first();

if (latest) {
  const goals = {
    "Жим лёжа": { current: latest.жим_лежа || 0, target: 60 },
    "Тяга штанги": { current: latest.тяга_штанги || 0, target: 60 },
    "Жим ногами": { current: latest.жим_ногами || 0, target: 130 },
    "Ягодичный мост": { current: latest.ягодичный_мост || 0, target: 85 },
    "Жим стоя": { current: latest.жим_стоя || 0, target: 40 },
    "Вес": { current: latest.вес || 0, target: 57.5 }
  };

  dv.header(3, "🎯 Общий прогресс к целям");
  
  Object.entries(goals).forEach(([exercise, data]) => {
    if (data.current > 0) {
      const progress = ((data.current / data.target) * 100).toFixed(1);
      const bar = "█".repeat(Math.floor(progress / 10)) + "░".repeat(10 - Math.floor(progress / 10));
      dv.paragraph(`**${exercise}:** ${data.current}/${data.target} (${progress}%) ${bar}`);
    }
  });
}
```

## 📝 Программа тренировок

### 🏋️‍♂️ Основные упражнения
1. **Жим лёжа (грудь, трицепс)**
   - Работа: 3-4 подхода × 6-10 повторений
   - Прогрессия: каждую 1-2 недели +2.5 кг

2. **Тяга штанги в наклоне (спина)**
   - Работа: 3-4 подхода × 8-12 повторений
   - Прогрессия: каждую 1-2 недели +2.5 кг

3. **Жим ногами (ноги)**
   - Работа: 3-4 подхода × 8-10 повторений
   - Прогрессия: каждую 1-2 недели +5 кг

4. **Ягодичный мост (ягодицы)**
   - Работа: 3-4 подхода × 10-12 повторений
   - Прогрессия: каждую 1-2 недели +5 кг

5. **Жим стоя (плечи)**
   - Работа: 3-4 подхода × 8-10 повторений
   - Прогрессия: каждую 1-2 недели +2.5 кг

### 🥊 Дополнительные упражнения
- **Кор (пресс):** планка 2-3 минуты, скручивания с блином
- **Скакалка:** 5-10 мин после зала
- **Растяжка:** после каждой тренировки

## 🔗 Связи
- [[📅 Трекер тренировок]]
- [[🥗 Трекер питания]]
- [[📊 Аналитика прогресса]]

## 📌 Заметки
<!-- Дополнительные заметки -->

## 🏷️ Теги
#веса #прогресс #сила #измерения

---
