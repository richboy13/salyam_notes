---
title: "Аналитика прогресса"
created: 2024-01-15
modified: 2024-01-15
---

# 📊 Аналитика прогресса

## 📋 Описание
Аналитика прогресса в тренировках с использованием Dataview для анализа данных из всех трекеров.

## 📈 Общая статистика

### 🏋️‍♂️ Тренировки
```dataviewjs
// Получаем последние тренировки
const workouts = dv.pages('"Dairy/Дневник/Тренировки"')
  .where(p => p.тренировка)
  .sort(p => p.file.name, 'desc')
  .limit(10);

dv.paragraph(`Найдено тренировок: ${workouts.length}`);

if (workouts.length > 0) {
  dv.paragraph("Последние тренировки:");
  for (let p of workouts) {
    const types = [];
    if (p.силовая_тренировка) types.push("Силовая");
    if (p.единоборства) types.push("Единоборства");
    if (p.плавание) types.push("Плавание");
    if (p.растяжка) types.push("Растяжка");
    dv.paragraph(`- ${p.file.name}: ${types.join(", ") || "Тренировка"}`);
  }
}
```

### 📊 Веса в упражнениях
```dataviewjs
// Получаем данные по весам
const weights = dv.pages('"Dairy/Дневник/Тренировки"')
  .where(p => p.жим_лежа || p.тяга_штанги || p.жим_ногами || p.ягодичный_мост || p.жим_стоя)
  .sort(p => p.file.name, 'desc')
  .limit(10);

dv.paragraph(`Найдено записей с весами: ${weights.length}`);

if (weights.length > 0) {
  dv.paragraph("Последние веса:");
  for (let p of weights) {
    const exercises = [];
    if (p.жим_лежа) exercises.push(`Жим лёжа: ${p.жим_лежа} кг`);
    if (p.тяга_штанги) exercises.push(`Тяга: ${p.тяга_штанги} кг`);
    if (p.жим_ногами) exercises.push(`Жим ногами: ${p.жим_ногами} кг`);
    if (p.ягодичный_мост) exercises.push(`Ягодичный мост: ${p.ягодичный_мост} кг`);
    if (p.жим_стоя) exercises.push(`Жим стоя: ${p.жим_стоя} кг`);
    dv.paragraph(`- ${p.file.name}: ${exercises.join(", ") || "Нет данных"}`);
  }
}
```

### ⚖️ Измерения тела
```dataviewjs
// Получаем данные по измерениям
const measurements = dv.pages('"Dairy/Дневник/Тренировки"')
  .where(p => p.вес || p.бицепс_левый || p.бицепс_правый || p.грудь || p.талия)
  .sort(p => p.file.name, 'desc')
  .limit(10);

dv.paragraph(`Найдено записей с измерениями: ${measurements.length}`);

if (measurements.length > 0) {
  dv.paragraph("Последние измерения:");
  for (let p of measurements) {
    const measures = [];
    if (p.вес) measures.push(`Вес: ${p.вес} кг`);
    if (p.бицепс_левый) measures.push(`Бицепс Л: ${p.бицепс_левый} см`);
    if (p.бицепс_правый) measures.push(`Бицепс П: ${p.бицепс_правый} см`);
    if (p.грудь) measures.push(`Грудь: ${p.грудь} см`);
    if (p.талия) measures.push(`Талия: ${p.талия} см`);
    dv.paragraph(`- ${p.file.name}: ${measures.join(", ") || "Нет данных"}`);
  }
}
```

## 🥗 Питание

### 📊 Приемы пищи
```dataviewjs
// Получаем данные по питанию
const nutrition = dv.pages('"Dairy/Дневник/Тренировки"')
  .where(p => p.завтрак || p.обед || p.ужин || p.перекус)
  .sort(p => p.file.name, 'desc')
  .limit(10);

dv.paragraph(`Найдено записей питания: ${nutrition.length}`);

if (nutrition.length > 0) {
  dv.paragraph("Последние приемы пищи:");
  for (let p of nutrition) {
    const meals = [];
    if (p.завтрак) meals.push("Завтрак");
    if (p.обед) meals.push("Обед");
    if (p.ужин) meals.push("Ужин");
    if (p.перекус) meals.push("Перекус");
    dv.paragraph(`- ${p.file.name}: ${meals.join(", ") || "Нет данных"}`);
  }
}
```

### 🥩 Макронутриенты
```dataviewjs
// Получаем данные по макронутриентам
const macros = dv.pages('"Dairy/Дневник/Тренировки"')
  .where(p => p.белки || p.углеводы || p.жиры || p.овощи)
  .sort(p => p.file.name, 'desc')
  .limit(10);

dv.paragraph(`Найдено записей с макронутриентами: ${macros.length}`);

if (macros.length > 0) {
  dv.paragraph("Последние макронутриенты:");
  for (let p of macros) {
    const macroList = [];
    if (p.белки) macroList.push(`Белки: ${p.белки} приема`);
    if (p.углеводы) macroList.push(`Углеводы: ${p.углеводы} приема`);
    if (p.жиры) macroList.push(`Жиры: ${p.жиры} приема`);
    if (p.овощи) macroList.push(`Овощи: ${p.овощи} приема`);
    dv.paragraph(`- ${p.file.name}: ${macroList.join(", ") || "Нет данных"}`);
  }
}
```

### ⚡ Добавки
```dataviewjs
// Получаем данные по добавкам
const supplements = dv.pages('"Dairy/Дневник/Тренировки"')
  .where(p => p.протеин || p.креатин || p.bcaa)
  .sort(p => p.file.name, 'desc')
  .limit(10);

dv.paragraph(`Найдено записей с добавками: ${supplements.length}`);

if (supplements.length > 0) {
  dv.paragraph("Последние добавки:");
  for (let p of supplements) {
    const supplementList = [];
    if (p.протеин) supplementList.push("Протеин");
    if (p.креатин) supplementList.push("Креатин");
    if (p.bcaa) supplementList.push("BCAA");
    dv.paragraph(`- ${p.file.name}: ${supplementList.join(", ") || "Нет добавок"}`);
  }
}
```

## 📊 Анализ прогресса

### 🎯 Прогресс к целям
- **Вес:** Анализ динамики веса к цели 57-58 кг
- **Жим лёжа:** Прогресс к цели 60 кг
- **Тяга штанги:** Прогресс к цели 60 кг
- **Жим ногами:** Прогресс к цели 130 кг
- **Ягодичный мост:** Прогресс к цели 85 кг
- **Жим стоя:** Прогресс к цели 40 кг

### 📈 Тренды
- **Регулярность тренировок:** Анализ частоты посещений
- **Прогрессия весов:** Динамика увеличения весов
- **Соблюдение питания:** Регулярность приемов пищи
- **Прием добавок:** Регулярность приема добавок

## 🔗 Связи
- [[📊 Трекер прогресса]]
- [[📈 Трекер весов]]
- [[📅 Трекер тренировок]]
- [[🥗 Трекер питания]]

## 📌 Заметки
<!-- Дополнительные заметки -->

## 🏷️ Теги
#аналитика #прогресс #статистика #dataview

---
