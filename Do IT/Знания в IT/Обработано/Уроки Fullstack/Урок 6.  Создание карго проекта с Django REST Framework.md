---
title: "Урок 6 - Создание карго проекта с Django REST Framework"
created: 2025-08-23
modified: 2025-08-23
---

# 📖 Урок 6 - Создание карго проекта с Django REST Framework

## 📋 Описание
[[Django REST Framework]] - Подробная шпаргалка для начинающих по созданию API.

## 🎯 Цели урока
<!-- Что изучим -->

## 📚 Материалы
<!-- Что понадобится -->

## 📝 Основные темы

### [[Django REST Framework]] - Подробная шпаргалка для начинающих

## 📁 Структура проекта

copyMyApiCargoProject/
├── apps/
│   ├── accounts/         # Приложение для управления пользователями
│   │   ├── migrations/   # Миграции базы данных
│   │   ├── __init__.py   # Инициализация пакета
│   │   ├── admin.py      # Настройки админ-панели
│   │   ├── apps.py       # Конфигурация приложения
│   │   ├── models.py     # Модели данных
│   │   ├── serializers.py # Сериализаторы для API
│   │   ├── tests.py      # Тесты
│   │   ├── urls.py       # URL маршруты
│   │   └── views.py      # Представления (обработчики запросов)
│   └── main/             # Основное приложение
└── core/                 # Основные настройки проекта
    ├── __init__.py       # Инициализация пакета
    ├── asgi.py           # ASGI конфигурация
    ├── settings.py       # Настройки проекта
    ├── urls.py           # Главные URL маршруты
    └── wsgi.py           # WSGI конфигурация

## 🔧 Основы Django REST Framework

### Что такое REST API?
REST (Representational State Transfer) - это архитектурный стиль для создания веб-сервисов. API (Application Programming Interface) - это интерфейс, который позволяет разным программам взаимодействовать друг с другом.

REST API использует:
- HTTP методы (GET, POST, PUT, DELETE)
- URL для идентификации ресурсов
- JSON или XML для передачи данных

### Основные HTTP методы:
- **GET** - получение данных
- **POST** - создание новых данных
- **PUT/PATCH** - обновление существующих данных
- **DELETE** - удаление данных

## 📝 Компоненты Django REST Framework

### 1. Модели (models.py)
Модели определяют структуру данных в базе данных.

```python
from django.db import models
from django.contrib.auth.models import AbstractUser

class User(AbstractUser):
    phone = models.CharField(max_length=15, blank=True)
    address = models.TextField(blank=True)
    
    def __str__(self):
        return self.username
```

### 2. Сериализаторы (serializers.py)
Сериализаторы преобразуют данные моделей в JSON и обратно.

```python
from rest_framework import serializers
from .models import User

class UserSerializer(serializers.ModelSerializer):
    class Meta:
        model = User
        fields = ['id', 'username', 'email', 'phone', 'address']
        extra_kwargs = {'password': {'write_only': True}}
    
    def create(self, validated_data):
        user = User.objects.create_user(**validated_data)
        return user
```

### 3. Представления (views.py)
Представления обрабатывают HTTP запросы и возвращают ответы.

#### Generic Views:
```python
from rest_framework import generics
from .models import User
from .serializers import UserSerializer

# Список пользователей и создание нового (GET, POST)
class UserListCreateView(generics.ListCreateAPIView):
    queryset = User.objects.all()
    serializer_class = UserSerializer

# Получение, обновление и удаление пользователя (GET, PUT, DELETE)
class UserDetailView(generics.RetrieveUpdateDestroyAPIView):
    queryset = User.objects.all()
    serializer_class = UserSerializer
```

### 4. URL маршруты (urls.py)
URL маршруты связывают URL с представлениями.

```python
# apps/accounts/urls.py
from django.urls import path
from . import views

urlpatterns = [
    path('users/', views.UserListCreateView.as_view(), name='user-list-create'),
    path('users/<int:pk>/', views.UserDetailView.as_view(), name='user-detail'),
]

# core/urls.py
from django.contrib import admin
from django.urls import path, include
from drf_yasg.views import get_schema_view
from drf_yasg import openapi
from rest_framework import permissions

# Настройка Swagger/OpenAPI
schema_view = get_schema_view(
    openapi.Info(
        title="Cargo API",
        default_version='v1',
        description="API для грузоперевозок",
    ),
    public=True,
    permission_classes=[permissions.AllowAny],
)

urlpatterns = [
    path('admin/', admin.site.urls),
    path('api/', include('apps.accounts.urls')),
    path('swagger/', schema_view.with_ui('swagger', cache_timeout=0), name='schema-swagger-ui'),
    path('redoc/', schema_view.with_ui('redoc', cache_timeout=0), name='schema-redoc'),
]
```

## 🚀 Пошаговое руководство по использованию

### Шаг 1: Установка необходимых пакетов
```bash
pip install django djangorestframework drf-yasg
```

### Шаг 2: Создание проекта и приложений
```bash
django-admin startproject core .
mkdir -p apps/accounts
django-admin startapp accounts apps/accounts
```

### Шаг 3: Настройка settings.py
```python
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    
    # Сторонние приложения
    'rest_framework',
    'drf_yasg',
    
    # Собственные приложения
    'apps.accounts',
    'apps.main',
]

# Настройки REST Framework
REST_FRAMEWORK = {
    'DEFAULT_PERMISSION_CLASSES': [
        'rest_framework.permissions.IsAuthenticated',
    ],
    'DEFAULT_AUTHENTICATION_CLASSES': [
        'rest_framework.authentication.SessionAuthentication',
        'rest_framework.authentication.BasicAuthentication',
    ],
}
```

### Шаг 4: Создание моделей
Определите модели в `apps/accounts/models.py`

### Шаг 5: Создание миграций и применение их
```bash
python manage.py makemigrations
python manage.py migrate
```

### Шаг 6: Создание сериализаторов
Создайте файл `apps/accounts/serializers.py` и определите сериализаторы

### Шаг 7: Создание представлений
Определите представления в `apps/accounts/views.py`

### Шаг 8: Настройка URL маршрутов
Создайте файлы `apps/accounts/urls.py` и обновите `core/urls.py`

### Шаг 9: Запуск сервера
```bash
python manage.py runserver
```

## 📚 Подробное объяснение Generic Views

### ListAPIView
Используется для получения списка объектов (GET запрос).
```python
class UserListView(generics.ListAPIView):
    queryset = User.objects.all()
    serializer_class = UserSerializer
```

### CreateAPIView
Используется для создания новых объектов (POST запрос).
```python
class UserCreateView(generics.CreateAPIView):
    serializer_class = UserSerializer
```

### ListCreateAPIView
Комбинация ListAPIView и CreateAPIView (GET и POST запросы).
```python
class UserListCreateView(generics.ListCreateAPIView):
    queryset = User.objects.all()
    serializer_class = UserSerializer
```

### RetrieveAPIView
Используется для получения одного объекта по ID (GET запрос).
```python
class UserDetailView(generics.RetrieveAPIView):
    queryset = User.objects.all()
    serializer_class = UserSerializer
```

### UpdateAPIView
Используется для обновления объекта (PUT и PATCH запросы).
```python
class UserUpdateView(generics.UpdateAPIView):
    queryset = User.objects.all()
    serializer_class = UserSerializer
```

### DestroyAPIView
Используется для удаления объекта (DELETE запрос).
```python
class UserDeleteView(generics.DestroyAPIView):
    queryset = User.objects.all()
    serializer_class = UserSerializer
```

### RetrieveUpdateDestroyAPIView
Комбинация RetrieveAPIView, UpdateAPIView и DestroyAPIView (GET, PUT, PATCH и DELETE запросы).
```python
class UserDetailView(generics.RetrieveUpdateDestroyAPIView):
    queryset = User.objects.all()
    serializer_class = UserSerializer
```

## 🔍 Тестирование API

### Использование Swagger UI
1. Откройте браузер и перейдите по адресу `http://localhost:8000/swagger/`
2. Вы увидите интерактивную документацию вашего API
3. Можно тестировать API прямо из браузера

### Использование Postman
1. Установите Postman (https://www.postman.com/)
2. Создайте новый запрос
3. Выберите метод (GET, POST, PUT, DELETE)
4. Введите URL (например, `http://localhost:8000/api/users/`)
5. Для POST и PUT запросов добавьте данные в формате JSON в тело запроса
6. Отправьте запрос и проверьте ответ

### Примеры запросов

#### Получение списка пользователей
```
GET http://localhost:8000/api/users/
```

#### Создание нового пользователя
```
POST http://localhost:8000/api/users/
Content-Type: application/json

{
    "username": "newuser",
    "email": "newuser@example.com",
    "password": "securepassword",
    "phone": "1234567890",
    "address": "123 Main St"
}
```

#### Получение данных пользователя
```
GET http://localhost:8000/api/users/1/
```

#### Обновление пользователя
```
PUT http://localhost:8000/api/users/1/
Content-Type: application/json

{
    "username": "updateduser",
    "email": "updated@example.com",
    "phone": "9876543210",
    "address": "456 New St"
}
```

#### Удаление пользователя
```
DELETE http://localhost:8000/api/users/1/
```

## 🛡️ Аутентификация и авторизация

### Базовая аутентификация
```python
# settings.py
REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': [
        'rest_framework.authentication.BasicAuthentication',
        'rest_framework.authentication.SessionAuthentication',
    ],
}
```

### Токен аутентификация
1. Добавьте 'rest_framework.authtoken' в INSTALLED_APPS
2. Выполните миграции
3. Настройте аутентификацию:
```python
# settings.py
REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': [
        'rest_framework.authentication.TokenAuthentication',
    ],
}
```

### Permissions (разрешения)
```python
from rest_framework.permissions import IsAuthenticated, IsAdminUser

class UserListCreateView(generics.ListCreateAPIView):
    queryset = User.objects.all()
    serializer_class = UserSerializer
    permission_classes = [IsAuthenticated]  # Только аутентифицированные пользователи
```

## 📌 Полезные советы

1. **Используйте фильтрацию**:
```python
from django_filters.rest_framework import DjangoFilterBackend

class UserListView(generics.ListAPIView):
    queryset = User.objects.all()
    serializer_class = UserSerializer
    filter_backends = [DjangoFilterBackend]
    filterset_fields = ['username', 'email']
```

2. **Добавьте пагинацию**:
```python
# settings.py
REST_FRAMEWORK = {
    'DEFAULT_PAGINATION_CLASS': 'rest_framework.pagination.PageNumberPagination',
    'PAGE_SIZE': 10
}
```

3. **Используйте вложенные сериализаторы**:
```python
class ProfileSerializer(serializers.ModelSerializer):
    class Meta:
        model = Profile
        fields = ['bio', 'avatar']

class UserSerializer(serializers.ModelSerializer):
    profile = ProfileSerializer()
    
    class Meta:
        model = User
        fields = ['id', 'username', 'email', 'profile']
```

4. **Добавьте валидацию**:
```python
class UserSerializer(serializers.ModelSerializer):
    class Meta:
        model = User
        fields = ['id', 'username', 'email', 'password']
    
    def validate_email(self, value):
        if not value.endswith('@example.com'):
            raise serializers.ValidationError("Email должен быть из домена example.com")
        return value
```

## 🔗 Полезные ссылки

- [Django документация](https://docs.djangoproject.com/)
- [Django REST Framework документация](https://www.django-rest-framework.org/)
- [drf-yasg документация](https://drf-yasg.readthedocs.io/en/stable/)
- [Руководство по REST API](https://restfulapi.net/)
- [HTTP методы и коды состояния](https://developer.mozilla.org/en-US/docs/Web/HTTP/Status)

## 💻 Практика
<!-- Практические задания -->

## 🔗 Связи
<!-- Связи с другими уроками -->

## 📌 Заметки
<!-- Дополнительные заметки -->

## 🏷️ Теги
#do_it #урок #django #python #rest #api #обучение

---