---
title: "Урок 1 - Создание проекта Django"
created: 2025-08-23
modified: 2025-08-23
---

# 📖 Урок 1 - Создание проекта Django

## 📋 Описание
Основные шаги для создания проекта [[Django]] и настройки базовой структуры.

## 🎯 Цели урока
<!-- Что изучим -->

## 📚 Материалы
<!-- Что понадобится -->

## 📝 Основные темы

### Основные шаги для создания проекта [[Django]]

### **Установка Django**

**1. Установка Django**
```bash
python3 -m pip install django
```

**2. Создание проекта**
```bash
cd путь/к/папке
django-admin startproject mysite
```

**3. Открытие в VS Code**
Откройте папку проекта в VS Code для удобной работы.

**4. Создание виртуального окружения**
```bash
python3 -m venv venv
source venv/bin/activate.fish
```

**5. Установка Django в проект**
```bash
pip install django
```

**6. Сохранение зависимостей**
```bash
pip freeze > requirements.txt
```

**7. Применение миграций**
```bash
python3 manage.py makemigrations
python3 manage.py migrate
```

**8. Запуск сервера разработки**
```bash
python3 manage.py runserver
```
Откройте браузер по адресу http://127.0.0.1:8000/

**9. Создание суперпользователя**
```bash
python3 manage.py createsuperuser
```
Укажите имя пользователя, email и пароль.

**10. Работа с админ-панелью**
- Перейдите в админ-панель по адресу http://127.0.0.1:8000/admin/
- Авторизуйтесь с учетными данными суперпользователя
- Создайте тестового пользователя в админке

  
### **Работа с приложением**

**11. Создание приложения**
```bash
python3 manage.py startapp telegram
```

**12. Настройка файла apps.py**
В файле apps.py приложения telegram настройте класс конфигурации:

```python
class TelegramConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'telegram'
    verbose_name = 'Telegram Bot Application'
```

**14. Создать модель базы данных**
В файле models.py приложения создать таблицу для хранения данных пользователей Telegram:
```python
class TelegramUser(models.Model):
    user_id = models.BigIntegerField(verbose_name="Id пользователя")
    username = models.CharField(verbose_name="Имя пользователя", max_length=255, null=True, blank=True)
    first_name = models.CharField(verbose_name="Имя", max_length=255, null=True, blank=True)
    last_name = models.CharField(verbose_name="Фамилия", max_length=255, null=True, blank=True)
    is_bot = models.BooleanField(verbose_name="Является ботом", default=False)
    language_code = models.CharField(verbose_name="Код языка", max_length=10, null=True, blank=True)
    created_at = models.DateTimeField(verbose_name="Дата создания", auto_now_add=True)
    updated_at = models.DateTimeField(verbose_name="Дата обновления", auto_now=True)
    is_active = models.BooleanField(verbose_name="Активен", default=True)


    def __str__(self):
        return f"{self.user_id}| {self.username} - {self.first_name} - {self.last_name}"
  

    class Meta:
        verbose_name = "Пользователь Telegram"
        verbose_name_plural = "Пользователи Telegram"
```
 

**14.1 Типы данных в моделях Django**

```python
BigIntegerField = int  # Поле для больших целых чисел
CharField, TextField = str  # Поля для текстовых данных (CharField — ограниченная длина, TextField — неограниченная)
BooleanField = bool  # Поле для логического значения (True/False)
DateTimeField = datetime  # Поле для хранения даты и времени
IntegerField, PositiveIntegerField, SmallIntegerField, PositiveSmallIntegerField = int  # Поля для целых чисел (с ограничением на знак и размер)
FloatField = float  # Поле для чисел с плавающей запятой
DecimalField = decimal.Decimal  # Поле для хранения десятичных чисел с фиксированной точностью
EmailField = str  # Поле для хранения email-адресов
URLField = str  # Поле для хранения URL-адресов
FileField, ImageField = str  # Поля для хранения путей к файлам/изображениям
ForeignKey, OneToOneField = связанная модель  # Поля для связи с другой моделью (один ко многим и один к одному)
ManyToManyField = QuerySet связанных моделей  # Поле для связи многие ко многим (хранит набор связанных объектов)
AutoField = int  # Поле для автогенерации идентификаторов (например, первичных ключей)
JSONField = dict или list  # Поле для хранения данных в формате JSON
UUIDField = uuid.UUID  # Поле для хранения UUID (уникальных идентификаторов)
SlugField = str  # Поле для хранения коротких текстов, часто используется для URL
TimeField = time  # Поле для хранения времени (без даты)
DurationField = timedelta  # Поле для хранения длительности (разницы во времени)
```


**15. Добавить модель в админ-панель**
В файле admin.py зарегистрировать модель TelegramUser:
```python
from telegram.models import TelegramUser

# Register your models here.

admin.site.register(TelegramUser)
```


**16. Подключить приложение к проекту**
Добавить приложение telegram в список INSTALLED_APPS в файле settings.py.


**17. Применить миграции для нового приложения**

```bash
python manage.py makemigrations  
python manage.py migrate
```

## 💻 Практика
<!-- Практические задания -->

## 🔗 Связи
<!-- Связи с другими уроками -->

## 📌 Заметки
<!-- Дополнительные заметки -->

## 🏷️ Теги
#do_it #урок #django #python #проект #обучение

---
