---
title: "Урок 3 - Импорт и Экспорт данных"
created: 2025-08-23
modified: 2025-08-23
---

# 📖 Урок 3 - Импорт и Экспорт данных

## 📋 Описание
Настройка Импорта и Экспорта данных из [[База данных|базы данных]] через админ панель.

## 🎯 Цели урока
<!-- Что изучим -->

## 📚 Материалы
<!-- Что понадобится -->

## 📝 Основные темы

### Настройка Импорта и Экспорта данных из [[База данных|базы данных]] через админ панель

**1. Установка библиотеки django-import-export**
Установка через pip:
```bash
pip install django-import-export
```
Эта библиотека добавляет возможность импорта и экспорта данных (например, в формате Excel, CSV, JSON) через административную панель. Это полезно для работы с большими объемами данных.

**2. Добавление import_export в INSTALLED_APPS**
```python
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'import_export',  # Подключили библиотеку
    'telegram',
]
```
Мы добавляем import_export, чтобы активировать возможности библиотеки в проекте. Без этого она не будет работать.

**3. Создание файла telegram/resources.py**
**Создание файла через терминал:**
```bash
touch telegram/resources.py
```
Этот файл нужен для определения “ресурсов” (классов) для импорта и экспорта данных. Здесь мы указываем, какие поля модели должны участвовать в экспорте/импорте и в каком порядке.

**Пример содержимого resources.py:**
```python
from telegram.models import TelegramUser
from import_export import resources

class TelegramUserResource(resources.ModelResource):
    class Meta:
        model = TelegramUser
        fields = ('user_id', 'username', 'first_name', 'last_name', 'is_bot', 'is_active', 'created_at', 'updated_at')
        export_order = ('user_id', 'username', 'first_name', 'last_name', 'is_bot', 'is_active', 'created_at', 'updated_at')
```
• fields: Поля, которые будут экспортироваться или импортироваться.
• export_order: Определяет порядок этих полей и возможность выбора полей для экспорта.

**4. Настройка admin.py для импорта и экспорта**
**Добавление библиотек:**
```python
from import_export.formats.base_formats import XLS, JSON
from import_export.admin import ImportExportModelAdmin, ExportMixin
from telegram.resources import TelegramUserResource
```
**Пример кода в admin.py:**
```python
from django.contrib import admin
from import_export.admin import ImportExportModelAdmin
from telegram.models import TelegramUser
from telegram.resources import TelegramUserResource

@admin.register(TelegramUser)
class TelegramUserAdmin(ImportExportModelAdmin):
    resource_class = TelegramUserResource
    list_display = ('user_id', 'username', 'first_name', 'last_name', 'is_bot', 'is_active', 'created_at', 'updated_at')
    search_fields = ('user_id', 'username', 'first_name', 'last_name')
    list_filter = ('is_bot', 'is_active', 'created_at', 'updated_at')
```
• ImportExportModelAdmin: Делает модель доступной для импорта и экспорта. Т.е. добавляет кастомно кнопки в админ панель. 
• resource_class: Подключает созданный ранее ресурс TelegramUserResource.
Теперь в админке можно загружать данные из файла или выгружать их.

## 💻 Практика
<!-- Практические задания -->

## 🔗 Связи
<!-- Связи с другими уроками -->

## 📌 Заметки
<!-- Дополнительные заметки -->

## 🏷️ Теги
#do_it #урок #django #python #импорт #экспорт #обучение

---


