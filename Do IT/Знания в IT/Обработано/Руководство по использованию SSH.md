---
title: "Руководство по использованию SSH"

created: 2025-08-23
modified: 2025-08-23
---

# 🔐 Руководство по использованию [[SSH]]

## 📋 Описание
Подробное руководство по настройке и использованию SSH для безопасного подключения к серверам.

## 🔑 Генерация ключей

> [!info] Создание SSH ключей
> Первый шаг для безопасного подключения к серверам

В зависимости от вашей операционной системы откройте терминал:
- **Linux** - используйте консоль
- **macOS** - запустите терминал
- **Windows** - воспользуйтесь PowerShell

Выполните команду:
```bash
ssh-keygen
```

> [!tip] Простая настройка
> Нажимайте Enter, не утруждая себя дополнительными вопросами.

В результате у вас появятся два файла в папке `.ssh` в вашем домашнем каталоге. Чтобы увидеть скрытую папку в Linux и macOS, используйте `ls -a`. В Windows включите отображение скрытых папок.

**Файлы в папке .ssh:**
- `id_rsa` (приватный ключ, хранится в тайне)
- `id_rsa.pub` (публичный ключ, который следует разместить на сервере)

## 🚀 Добавление ключа на сервер

### 🆕 Для нового сервера:
> [!success] Новый сервер
> Настройка SSH для только что созданного сервера

1. Перейдите во вкладку "Серверы"
2. Выберите "SSH" в верхнем меню
3. Нажмите "Добавить SSH ключ", введите имя ключа и сам ключ, сохраните

### 🔧 Для уже существующего сервера:
> [!warning] Существующий сервер
> Настройка SSH для уже работающего сервера

1. В панели управления серверами выберите "Действия" -> "Безопасность" -> "Изменить root пароль". Создайте пароль
2. Подключитесь к серверу используя пароль
3. На сервере создайте папку .ssh: `mkdir .ssh` и перейдите в нее: `cd .ssh`
4. Создайте файл с ключами `authorized_keys`: `nano authorized_keys`
5. Вставьте туда публичный ключ, сохраните и выйдите (CTRL + X, Enter)

> [!tip] Проверка
> Проверьте подключение – если пароль не требуется, всё в порядке

### 💻 Для Linux/Mac (локальный компьютер):
> [!info] Автоматическая настройка
> Простой способ для Linux и macOS

Выполните команду:
```bash
ssh-copy-id user@server
```
Где `user` – имя пользователя, `server` – IP-адрес или домен сервера.

## ⚙️ Изменение конфигурации SSH на сервере

### 🔄 Обновление потока сокета прослушивания:
> [!note] Настройка портов
> Изменение порта для SSH подключения

```bash
mkdir -p /etc/systemd/system/ssh.socket.d
```

```bash
cat >/etc/systemd/system/ssh.socket.d/listen.conf <<EOF
    [Socket]
    ListenStream=
    ListenStream=1234
    EOF
```

```bash
sudo systemctl daemon-reload
```

```bash
sudo systemctl restart ssh.socket
```

Проверить, что начал прослушивать новый порт:
```bash
systemctl status ssh
```

### 🔒 Отключение аутентификации по паролю:
> [!warning] Безопасность
> Отключение парольной аутентификации для повышения безопасности

- `PasswordAuthentication yes` на `PasswordAuthentication no`

Сохраните и закройте (CTRL + X, Enter).

Отредактируйте файл `/etc/ssh/sshd_config.d/50-cloud-init.conf`:
```bash
nano /etc/ssh/sshd_config.d/50-cloud-init.conf
```

Измените `PasswordAuthentication yes` на `PasswordAuthentication no`. Сохраните и закройте.

Перезапустите службу SSH:
```bash
service ssh restart
```

> [!tip] Проверка
> Проверьте подключение – если работает, переходите к следующему шагу

## 👤 Запрет логина root на сервер

> [!warning] Безопасность root
> Создание отдельного пользователя для повышения безопасности

Создайте нового пользователя:
```bash
adduser username
```
Где `username` – имя нового пользователя. Задайте ему пароль.

Создайте папку и файл с ключами:
```bash
mkdir /home/username/.ssh
nano /home/username/.ssh/authorized_keys
```

## 🔗 Связи
- [[SSH]]
- [[сервер]]
- [[безопасность]]
- [[ключи]]

## 📌 Заметки
<!-- Дополнительные заметки -->

## 🏷️ Теги
#do_it #ssh #безопасность #сервер #ключи #руководство

---