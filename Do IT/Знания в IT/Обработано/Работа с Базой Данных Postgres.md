---
title: "Работа с Базой Данных PostgreSQL"
created: 2025-08-23
modified: 2025-08-23
---

# 🗄️ Работа с [[База данных|Базой Данных]] [[Postgres]]

## 📋 Описание
Руководство по работе с PostgreSQL: создание базы данных, основные SQL команды и операции.

## 🚀 Создание БД

> [!info] Настройка базы данных
> Первые шаги для работы с PostgreSQL

1. **pgAdmin4** - создаем базу данных (БД)
2. **DBeaver** - соединяемся с созданной БД

## 💻 Основные команды

### 📝 INSERT INTO
> [!tip] Вставка данных
> Вставляет новую строку данных в таблицу с указанием значений для заданных колонок.

```sql
-- Вставка данных в таблицу
INSERT INTO table_name (column1, column2) VALUES (value1, value2);
```

### 🔍 SELECT
> [!success] Выборка данных
> Извлекает данные из таблицы в соответствии с заданным условием.

```sql
-- Выборка данных из таблицы с определённым условием
SELECT column1, column2 FROM table_name WHERE condition;
```

### ✏️ UPDATE
> [!note] Обновление данных
> Обновляет значения определённой колонки в таблице согласно заданному условию.

```sql
-- Обновление данных в таблице с определённым условием
UPDATE table_name SET column1 = value1 WHERE condition;
```

### 🗑️ DELETE
> [!warning] Удаление данных
> Удаляет строки данных из таблицы в соответствии с заданным условием.

```sql
-- Удаление данных из таблицы с определённым условием
DELETE FROM table_name WHERE condition;
```

### ➕ ALTER TABLE ADD COLUMN
> [!info] Добавление колонки
> Добавляет новую колонку в существующую таблицу.

```sql
-- Добавление новой колонки в таблицу
ALTER TABLE table_name ADD column_name datatype;
```

### 🔄 ALTER TABLE ALTER COLUMN TYPE
> [!tip] Изменение типа данных
> Изменяет тип данных существующей колонки в таблице.

```sql
-- Изменение типа данных в существующей колонке таблицы
ALTER TABLE table_name ALTER COLUMN column_name TYPE new_datatype;
```

### ❌ DROP TABLE
> [!warning] Удаление таблицы
> Удаляет всю таблицу из базы данных.

```sql
-- Удаление таблицы
DROP TABLE table_name;
```

### 🔄 UPDATE с условием
> [!success] Обновление с условием
> Обновляет данные в таблице согласно заданному условию.

```sql
-- Обновление данных в таблице с определённым условием, используя значения из другой колонки
UPDATE table_name SET column3 = 900 WHERE column1 IN ('name1', 'name3');
```

### ➖ ALTER TABLE DROP COLUMN
> [!warning] Удаление колонки
> Удаляет указанную колонку из таблицы.

```sql
-- Удаление колонки из таблицы
ALTER TABLE table_name DROP COLUMN column1;
```

## 🏗️ Создание таблиц

### 🔢 SERIAL
> [!info] Автоинкремент
> Создает колонку автоинкрементируемых целых чисел.

```sql
CREATE TABLE table1 (
    table1_id SERIAL PRIMARY KEY,
    name VARCHAR(255)
);
```

### 🔑 PRIMARY KEY
> [!success] Первичный ключ
> Определяет колонку или группу колонок в таблице, которые уникально идентифицируют каждую запись в таблице.

```sql
CREATE TABLE table1 (
    table1_id SERIAL PRIMARY KEY,
    name VARCHAR(255)
);
```

### 🔗 FOREIGN KEY
> [!tip] Внешний ключ
> Определяет связь между двумя таблицами. Значение в столбце этого ключа должно существовать в столбце, указанном внешним ключом в связанной таблице.

```sql
CREATE TABLE table2 (
    table2_id SERIAL PRIMARY KEY, 
    table1_id INT,
    FOREIGN KEY (table1_id) REFERENCES customers(table1_id)
);
```

## 🔗 JOIN операции

### 🔗 JOIN
> [!info] Внутреннее соединение
> Используется для объединения строк из двух или более таблиц на основе определенного условия.

```sql
SELECT * FROM table1
JOIN table2 ON table1.column_name = table2.column_name;
```

### ⬅️ LEFT JOIN
> [!tip] Левое соединение
> Возвращает все строки из левой таблицы и соответствующие строки из правой таблицы. Если в правой таблице нет соответствия, то для этих строк возвращается NULL.

```sql
SELECT * FROM table1
LEFT JOIN table2 ON table1.column_name = table2.column_name;
```

### ➡️ RIGHT JOIN
> [!note] Правое соединение
> Возвращает все строки из правой таблицы и соответствующие строки из левой таблицы. Если в левой таблице нет соответствия, то для этих строк возвращается NULL.

```sql
SELECT * FROM table1
RIGHT JOIN table2 ON table1.column_name = table2.column_name;
```

## 🔗 Связи
- [[База данных]]
- [[Postgres]]
- [[SQL]]
- [[Django]]

## 📌 Заметки
<!-- Дополнительные заметки -->

## 🏷️ Теги
#do_it #postgres #база_данных #sql #database #руководство

---

